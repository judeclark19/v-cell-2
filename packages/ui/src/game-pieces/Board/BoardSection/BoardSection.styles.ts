"use client";

import styled from "styled-components";

export const BoardTopRoot = styled.div`
  margin-bottom: var(--card-gap, 10px);
`;

export const BoardBottomRoot = styled.div`
  margin-top: var(--card-gap, 10px);
  position: relative;
`;
